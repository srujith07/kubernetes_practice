apiVersion: v1
kind: Service
metadata:
  name: result-svc
  labels:
    svcinfo: svc-result

spec:
  selector:
    podinfo: pod-result
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005
