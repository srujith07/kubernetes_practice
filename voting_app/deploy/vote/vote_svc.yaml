apiVersion: v1
kind: Service
metadata:
  name: vote-svc
  labels:
    svcinfo: svc-vote
spec:
  selector:
    podinfo: pod-vote
  type: NodePort

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30004
